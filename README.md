# CSS Specificity Bug

This repository demonstrates a subtle bug related to CSS specificity and selector order.  The bug showcases how the order of CSS rules with equal specificity can lead to unexpected results.  The solution shows how to modify the code to achieve the desired color.  This issue can be difficult to diagnose without a thorough understanding of CSS specificity.